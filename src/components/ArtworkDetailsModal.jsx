// Placeholder modal component - will implement with radix-ui dialog later
export const ArtworkDetailsModal = () => {
  // For now, just return null - we can implement the full modal later
  // This prevents the Layout component from throwing errors
  return null;
};